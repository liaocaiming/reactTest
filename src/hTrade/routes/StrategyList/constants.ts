import { isOrNot } from "@utils/lib/constants";

export const rowObj = {
  start: {
    type: "select",
    list: isOrNot,
  },
  push_bot: {
    type: "select",
    list: isOrNot,
  },
  push_soso: {
    type: "select",
    list: isOrNot,
  },
  push_app: {
    type: "select",
    list: isOrNot,
  }
}